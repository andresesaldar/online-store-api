group = 'com.test.online.store.common'

dependencies {
    implementation project(":common-model")

    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
}

bootJar {
    enabled = false
}